import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "RMGD Dashboard - Retro Mobile Gaming Database",
  description: "Admin dashboard for managing retro mobile games database (1975-2008)",
  keywords: "retro games, mobile gaming, database, preservation, research",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.className} antialiased`}>
        {children}
      </body>
    </html>
  );
}